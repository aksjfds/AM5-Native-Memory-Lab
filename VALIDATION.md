# 验证记录

验证日期：2026-09-15。

## 已完成

1. Windows x86-64 原生 EXE：Clang 17 + LLD 成功交叉编译，无编译警告。
2. 结构检查：PE32+、AMD64、ASLR/NX 标记、正确入口与导入表。Windows ABI 结构大小有编译期断言。
3. 导入依赖仅为 Windows 自带 `KERNEL32.dll`、`SHELL32.dll`、`WEVTAPI.dll`、`MSVCRT.dll`。没有 Python、Visual C++ Redistributable 或其他随包 DLL 的依赖。
4. 共享原生测试代码在 Linux x86-64 执行了 56 项内核/数学自检；包括 AVX2 Read、Copy、Write、混合读写的真实结果校验，随机指针环的完整性/访问计数，以及故意破坏数据后的检出能力。
5. Linux 完整实测流程：256 MiB 单数组、5 个可用 CPU、每负载 5 轮，180 个原始样本，全流程完成。开发环境约用时 94 秒；不是用户机器上的时间保证。没有观察到数据不一致。
6. 独立复核：882 个数据、字节/时间换算、CSV/JSON 一致性、命令行错误处理、PE 结构等断言通过。这不是 882 种独立硬件压力测试。
7. AddressSanitizer + UndefinedBehaviorSanitizer：自检和小工作集端到端流程均完成，未报告内存越界、未定义行为或内存泄漏。
8. 中止处理：SIGINT 后退出码 2，所有工作线程退出，JSON 标记 complete=false，已完成样本被保存。
9. Headless Chromium：14 项界面/计算/离线导出/窄屏显示断言通过，页面无 JavaScript 错误、无网络请求。因当前容器策略阻止 file:// 导航，页面通过内存文档载入方式渲染检查；没有修改安全策略。
10. 人工检查了报告桌面布局与内容，未见文字遮挡或表格溢出。

## 未完成，不能暗示已经完成

- 没有 Windows 运行环境，因此没有在 Windows 内实际启动过生成的 EXE。
- 没有在用户的 Ryzen 7 9700X + MSI B850 MPOWER + 单条 24GB M-die 上实测。
- Windows 处理器拓扑、QPC、Unicode 文件路径、线程 API 和 WHEA 查询经过文档核对、静态 ABI 检查与编译，但仍需要实际 Windows 兼容性确认。
- 没有数码签名，没有第三方安全审计；不要因此关闭系统防护。
- 程序自检通过不代表硬件超频稳定。

## 设计中明确不提供的能力

没有实际 BIOS 时序/电压读取、温度读取、IBS/UMC 计数器、Bank/行物理映射，也没有自动修改参数。诊断由当前运行的主动负载对照形成，定位到访问行为及候选参数组；不能宣称单次测试求出了每一个时序的最佳值。

完整源代码、测试脚本与开发环境测试记录保存在 source/。开发数据明确来自 Linux 环境，不能拿它们作为用户电脑的参考分数。
